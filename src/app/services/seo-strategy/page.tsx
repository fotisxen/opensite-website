import { SeoStrategyPage } from "@/components/pages/SeoStrategyPage";

export const metadata = {
  title: "SEO Strategy | OpenSite",
  description:
    "Visibility is infrastructure, not luck — technical audits, keyword architecture, content systems, and authority building.",
};

export default function Page() {
  return <SeoStrategyPage />;
}
